# stemformulas.com
The only place to look for STEM formulas




## Running locally:

Install Hugo

To run the site locally:
`hugo server`


To update the theme:
`git submodule update --remote --merge`
